{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "run": "php artisan serve",
        "clear": "php artisan view:clear && php artisan cache:clear && php artisan route:clear && php artisan config:clear"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    }
}
